1. The UI is built for tracking real-time position of the quadrotor swarm.
2. Python is used as the development environment for building the GUI on a Windows system.
3. The UI is built using the PyQt framework and the graphs are plotted using PyQtGraph embedded in PyQt.
